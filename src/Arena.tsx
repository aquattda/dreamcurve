import { createContext, useCallback, useContext, useEffect, useState } from 'react';
import { Link, NavLink, Route, Routes, useSearchParams } from 'react-router-dom';
import { ArrowRight, Clock3, Download, ExternalLink, Menu, RefreshCw, ShieldCheck, Wallet, X } from 'lucide-react';
import type { Address } from 'viem';
import { formatUnits } from 'viem';
import { AGENTS, blockReason, midpoint, type AgentId, type ArenaState, type Forecast, type Market, type Side } from '../shared/domain';
import { Logo } from './App';
import { cancelOrder, connectWallet, loadPortfolio, placeStake, redeemAll } from './wallet-bridge';
import type { Portfolio } from '@somnia-chain/markets-sdk';
import { ProbabilityChart } from './ProbabilityChart';

const money=new Intl.NumberFormat('en-US',{style:'currency',currency:'USD',maximumFractionDigits:2});
const pct=(n:number|null,d=0)=>n===null?'—':`${(n*100).toFixed(d)}%`;
const cents=(n:number|null|undefined,d=1)=>n==null?'No ask':`${(n*100).toFixed(d)}¢`;
type WalletCtx={account:Address|null;connect:()=>void};
const WalletContext=createContext<WalletCtx>({account:null,connect:()=>{}});
function useWalletContext(){return useContext(WalletContext)}
function useCountdown(expiry:number){const [now,setNow]=useState(Date.now());useEffect(()=>{const t=setInterval(()=>setNow(Date.now()),1000);return()=>clearInterval(t)},[]);const seconds=Math.max(0,Math.ceil((expiry-now)/1000));return seconds===0?'Closing':`${String(Math.floor(seconds/60)).padStart(2,'0')}:${String(seconds%60).padStart(2,'0')}`;}
function useArena(){const [params]=useSearchParams();const mode=params.get('mode')==='demo'?'demo':'live';const [state,setState]=useState<ArenaState|null>(null);const [error,setError]=useState('');const [retrying,setRetrying]=useState(false);const load=useCallback(async()=>{try{const r=await fetch(`/api/arena?mode=${mode}`,{cache:'no-store'});if(!r.ok)throw new Error(`API returned ${r.status}`);setState(await r.json());setError('')}catch(e){setError(e instanceof Error?e.message:'Could not reach the data service')}},[mode]);const retry=useCallback(async()=>{setRetrying(true);try{const r=await fetch('/api/retry',{method:'POST'});const next=await r.json() as ArenaState;if(next?.mode)setState(next);if(!r.ok)throw new Error(r.status===429?'A retry is already scheduled. Please wait a few seconds.':'The retry could not be started.');setError('')}catch(e){setError(e instanceof Error?e.message:'The retry could not be started.')}finally{setRetrying(false)}},[]);useEffect(()=>{void load();const t=setInterval(load,5000);return()=>clearInterval(t)},[load]);return{state,error,mode,load,retry,retrying};}
function AppHeader({account,onConnect,mode}:{account:Address|null;onConnect:()=>void;mode:string}){const [open,setOpen]=useState(false);const suffix=mode==='demo'?'?mode=demo':'';return <><header className="app-header"><Logo/><button className="mobile-menu" onClick={()=>setOpen(v=>!v)} aria-label="Open navigation"><Menu/></button><nav className={open?'open':''}><NavLink end to={`/app${suffix}`}>Arena</NavLink><NavLink to={`/app/agents${suffix}`}>Agents</NavLink><NavLink to={`/app/leaderboard${suffix}`}>Scorecard</NavLink><NavLink to={`/app/portfolio${suffix}`}>Portfolio</NavLink></nav><div className="app-actions"><span className="network-chip"><span className="status-dot"/> Shannon testnet</span><button className="button dark small" onClick={onConnect}><Wallet size={15}/>{account?`${account.slice(0,6)}…${account.slice(-4)}`:'Connect wallet'}</button></div></header>{open?<button className="menu-shade" aria-label="Close navigation" onClick={()=>setOpen(false)}/>:null}</>}
function DataBanner({state,error,mode}:{state:ArenaState|null;error:string;mode:string}){return <aside aria-label="Data status" className={`data-banner ${mode==='demo'?'demo':''}`}><span>{mode==='demo'?'ILLUSTRATIVE MODE':state?.status==='healthy'?'LIVE TESTNET':'CONNECTION STATUS'}</span><p>{error||state?.message||'Opening the DreamDEX data stream…'}</p>{mode==='demo'?<Link to="/app?mode=live">Try live testnet <ArrowRight size={14}/></Link>:<Link to="/app?mode=demo">Explore with example data <ArrowRight size={14}/></Link>}</aside>}
function AgentAvatar({id,large=false}:{id:AgentId;large?:boolean}){const a=AGENTS.find(x=>x.id===id)!;return <span aria-hidden="true" className={`agent-avatar ${a.color} ${large?'large':''}`}>{a.initial}<span>✳</span></span>}
function MarketRow({market,active,onClick}:{market:Market;active:boolean;onClick:()=>void}){const time=useCountdown(market.expiry);const mid=midpoint(market);return <button className={`market-row ${active?'active':''}`} onClick={onClick}><span aria-hidden="true" className={`coin ${market.asset==='BTC'?'bitcoin':'ethereum'}`}>{market.asset==='BTC'?'₿':'Ξ'}</span><span className="market-row-main"><strong>{market.asset} · {Math.round(market.interval/60)}m</strong><small>{market.strike?`Above ${money.format(market.strike)}`:'Opening reference'}</small></span><span className="market-row-odds"><strong>{pct(mid)}</strong><small>YES</small></span><span className="market-row-time"><Clock3 size={13}/>{time}</span></button>}
function AgentCard({forecast,onTrade,allowTrade,unavailableLabel='Illustration only'}:{forecast:Forecast;onTrade:(side:Side)=>void;allowTrade:boolean;unavailableLabel?:string}){const a=AGENTS.find(x=>x.id===forecast.agentId)!;const side=forecast.action==='BUY_NO'?'NO':'YES';return <article className={`arena-agent ${forecast.agentId==='meta'?'featured':''}`}><div className="arena-agent-head"><AgentAvatar id={forecast.agentId}/><div><strong>{a.name}</strong><span>{a.role}</span></div><span className={`confidence ${forecast.confidence.toLowerCase()}`}>{forecast.confidence} confidence</span></div><div className="forecast-value"><span>Forecast</span><strong>{pct(forecast.probabilityYes)}</strong><small>YES</small></div><div className={`signal-pill ${forecast.action==='NO_TRADE'?'neutral':side.toLowerCase()}`}>{forecast.action==='NO_TRADE'?'WAITING FOR AN EDGE':`${forecast.action.replace('_',' ')} · ${forecast.edge!==null?(forecast.edge*100).toFixed(1):'—'} PP EDGE`}</div><ul>{forecast.reasons.slice(0,3).map(r=><li key={r}>{r}</li>)}</ul><button className="button agent-button" disabled={forecast.action==='NO_TRADE'||!allowTrade} onClick={()=>onTrade(side)}>{allowTrade?'Copy this perspective':unavailableLabel} <ArrowRight size={15}/></button></article>}
function TradeModal({market,side,account,onConnect,onClose}:{market:Market;side:Side;account:Address|null;onConnect:()=>void;onClose:()=>void}) {
  const [stake,setStake]=useState('10');
  const [status,setStatus]=useState('');
  const [busy,setBusy]=useState(false);
  const [hash,setHash]=useState('');
  const [quote,setQuote]=useState<{shares:string;maxCost:string;limit:string}|null>(null);
  const blocked=blockReason(market);
  const approx=market[side==='YES'?'yesAsks':'noAsks'][0]?.price;
  async function submit(){if(!account){onConnect();return}setBusy(true);setStatus('Refreshing the order book and checking market status…');try{const r=await placeStake(market,side,stake,account,q=>{setQuote(q);setStatus('Quote ready. Confirm the wallet request to continue…')});setHash(r.hash);setStatus(r.fills?`Confirmed with ${r.fills} fill${r.fills===1?'':'s'}.`:'Confirmed. The order may have rested without a fill. Check Portfolio.')}catch(e){setStatus(e instanceof Error?e.message:'The trade could not be completed.')}finally{setBusy(false)}}
  return <div className="modal-backdrop" role="presentation" onMouseDown={e=>{if(e.target===e.currentTarget)onClose()}}><section className="trade-modal" role="dialog" aria-modal="true" aria-labelledby="trade-title"><button className="modal-close" onClick={onClose} aria-label="Close"><X/></button><span className="eyebrow">REVIEW YOUR MOVE</span><h2 id="trade-title">Buy {side} on {market.asset}</h2><p>{market.title}</p><div className="ticket-odds"><span>Current executable ask</span><strong>{cents(approx)}</strong></div><label>Maximum stake (tUSDC)<input value={stake} onChange={e=>setStake(e.target.value)} inputMode="decimal" pattern="[0-9]+([.][0-9]+)?" aria-describedby="stake-help"/></label><small id="stake-help">The final quote uses current on-chain depth, a 2% protective limit and the market’s tick/lot grid.</small>{quote?<div className="quote-grid"><span>Shares <strong>{Number(quote.shares).toFixed(2)}</strong></span><span>Max cost <strong>{quote.maxCost} tUSDC</strong></span><span>Limit <strong>{cents(Number(quote.limit))}</strong></span></div>:null}<div className="ticket-warning"><ShieldCheck size={19}/><span>Your wallet signs every transaction. Approval may require an extra confirmation.</span></div>{blocked?<p className="form-error">{blocked}</p>:null}{status?<p className={hash?'form-success':'form-status'}>{status}</p>:null}{hash?<a className="text-link" href={`https://shannon-explorer.somnia.network/tx/${hash}`} target="_blank" rel="noreferrer">View transaction <ExternalLink size={14}/></a>:null}<button className="button primary full" onClick={submit} disabled={busy||!!blocked||!/^\d+(\.\d+)?$/.test(stake)||Number(stake)<=0}>{busy?<><RefreshCw className="spin"/> Checking quote…</>:account?`Review ${side} trade`:'Connect wallet to continue'}</button><p className="fine-print">Testnet event contract. An outcome share can settle at zero. Quotes and forecasts are estimates.</p></section></div>
}
function ArenaHome({state,onRetry,retrying}:{state:ArenaState;onRetry:()=>void;retrying:boolean}) {
  const [selected,setSelected]=useState(state.markets[0]?.id||'');
  const [trade,setTrade]=useState<Side|null>(null);
  useEffect(()=>{if(!state.markets.some(m=>m.id===selected))setSelected(state.markets[0]?.id||'')},[state.markets,selected]);
  const market=state.markets.find(m=>m.id===selected);
  const {account,connect}=useWalletContext();
  if(!market&&state.mode==='live'&&state.status==='connecting')return <section className="loading-state"><RefreshCw className="spin"/><span>Opening the DreamDEX data stream…</span></section>;
  if(!market&&state.mode==='live'&&state.status==='degraded')return <LiveDataUnavailable state={state} onRetry={onRetry} retrying={retrying}/>;
  if(!market)return <EmptyState live={state.mode==='live'}/>;
  const forecasts=state.forecasts.filter(f=>f.marketId===market.id);
  const allowTrade=market.source==='live'&&state.status==='healthy'&&blockReason(market)===null;
  return <>
    <div className="arena-layout">
      <aside className="market-panel"><div className="panel-title"><span>Live windows</span><small>{state.markets.length} markets</small></div>{state.markets.map(m=><MarketRow key={m.id} market={m} active={m.id===market.id} onClick={()=>setSelected(m.id)}/>)}</aside>
      <section className="market-detail">
        <div className="market-title-row"><div><span className="eyebrow">{market.asset} · {Math.round(market.interval/60)} MINUTE WINDOW</span><h1>{market.title}</h1><p>{market.strike?`Strike ${money.format(market.strike)} · Spot ${market.spot?money.format(market.spot):'unavailable'}`:'Waiting for the opening reference price'}</p></div><div className="market-big-odds"><span>Market says</span><strong>{pct(midpoint(market))}</strong><small>YES</small></div></div>
        <div className="chart-card">
          <div className="chart-head"><span>Implied YES probability <small>Mid price</small></span><span><span className="status-dot"/> {market.source==='live'?'On-chain order book':'Illustrative series'}</span></div>
          <ProbabilityChart history={state.histories[market.id]||[]}/>
          <div className="book-tops">
            <button onClick={()=>setTrade('YES')} disabled={!allowTrade}><span>Buy YES<small>Contract price</small></span><strong>{cents(market.yesAsks[0]?.price)}</strong></button>
            <button onClick={()=>setTrade('NO')} disabled={!allowTrade}><span>Buy NO<small>Contract price</small></span><strong>{cents(market.noAsks[0]?.price)}</strong></button>
          </div>
        </div>
        <div className="arena-heading"><div><span className="eyebrow">FOUR SECOND OPINIONS</span><h2>Where the agents stand</h2></div><Link to="/methodology" className="text-link">Read the method <ArrowRight size={15}/></Link></div>
        <div className="agent-grid">{forecasts.map(f=><AgentCard key={f.id} forecast={f} allowTrade={allowTrade} unavailableLabel={market.source==='live'?'Trading paused':'Illustration only'} onTrade={s=>setTrade(s)}/>)}</div>
      </section>
    </div>
    {trade?<TradeModal market={market} side={trade} account={account} onConnect={connect} onClose={()=>setTrade(null)}/>:null}
  </>;
}
function LiveDataUnavailable({state,onRetry,retrying}:{state:ArenaState;onRetry:()=>void;retrying:boolean}){const timeout=state.issue==='INDEXER_TIMEOUT';return <section className="empty-state unavailable-state"><span className="empty-glyph error" aria-hidden="true">!</span><span className="eyebrow">LIVE DATA PAUSED</span><h1>Live market data is temporarily unavailable</h1><p>{timeout?'The DreamDEX testnet indexer did not respond in time. Your wallet and funds are not affected.':'DreamDEX or the Somnia testnet is not responding right now. Your wallet and funds are not affected.'}</p>{state.lastSuccessfulAt?<p className="retry-status">Last successful update: {new Date(state.lastSuccessfulAt).toLocaleString()}</p>:<p className="retry-status">No live snapshot has been received yet.</p>}<div className="empty-actions"><button className="button primary" onClick={onRetry} disabled={retrying||!state.retryable}>{retrying?<><RefreshCw className="spin" size={16}/> Retrying…</>:<><RefreshCw size={16}/> Retry live data</>}</button><Link to="/app?mode=demo" className="button outline">Explore example arena <ArrowRight size={16}/></Link></div></section>}
function EmptyState({live}:{live:boolean}){return <section className="empty-state"><span className="empty-glyph">⌁</span><h1>{live?'No active BTC or ETH windows':'No example markets available'}</h1><p>{live?'The collector is connected but did not find a currently tradable window. New Event Contracts appear automatically.':'Refresh the example arena.'}</p>{live?<Link to="/app?mode=demo" className="button primary">Explore example arena <ArrowRight size={16}/></Link>:null}</section>}
function AgentsPage({state}:{state:ArenaState}){return <section className="inner-page"><span className="eyebrow">OPEN METHODOLOGY</span><h1>Meet your second opinions.</h1><p className="page-lead">Four models turn the same market into four visible perspectives. Sample counts matter, and insufficient input always means “wait”.</p><div className="agents-full">{AGENTS.map(a=>{const score=state.scores.find(s=>s.agentId===a.id);return <article key={a.id}><AgentAvatar id={a.id} large/><span className="agent-role">{a.role.toUpperCase()}</span><h2>{a.name}</h2><p>{a.description}</p><dl><div><dt>Brier</dt><dd>{score?.brier===null||score?.brier===undefined?'—':score.brier.toFixed(3)}</dd></div><div><dt>Scored</dt><dd>{score?.count||0}</dd></div></dl><Link className="text-link" to="/methodology">View methodology <ArrowRight size={15}/></Link></article>})}</div></section>}
function Scorecard({state}:{state:ArenaState}){return <section className="inner-page"><div className="page-title-actions"><div><span className="eyebrow">ACCOUNTABILITY IS THE EDGE</span><h1>The open scorecard.</h1><p className="page-lead">Lower Brier scores mean confidence tracked outcomes more closely. Paper PnL assumes one share at the captured ask before fees and gas.</p></div>{state.mode==='live'?<a className="button outline" href="/api/proofs"><Download size={16}/> Export records</a>:null}</div><div className="score-table" role="table"><div className="score-row score-head" role="row"><span>Agent</span><span>Brier ↓</span><span>Hit rate</span><span>Paper PnL</span><span>Sample</span></div>{[...state.scores].sort((a,b)=>(a.brier??99)-(b.brier??99)).map((s,i)=>{const a=AGENTS.find(x=>x.id===s.agentId)!;return <div className="score-row" role="row" key={s.agentId}><span><b>{i+1}</b><AgentAvatar id={s.agentId}/><span><strong>{a.name}</strong><small>{a.role}</small></span></span><span>{s.brier===null?'Warming up':s.brier.toFixed(3)}</span><span>{s.hitRate===null?'—':pct(s.hitRate)}</span><span className={s.paperPnl>=0?'positive':'negative'}>{s.count?`${s.paperPnl>=0?'+':''}${s.paperPnl.toFixed(2)} tUSDC`:'—'}</span><span>{s.count} forecasts</span></div>})}</div><div className="proof-list"><div className="panel-title"><span>Resolved forecast record</span><small>{state.mode==='demo'?'Synthetic examples':'Recorded before settlement'}</small></div>{state.proofs.slice(0,12).map(p=><article key={`${p.marketId}-${p.agentId}`}><AgentAvatar id={p.agentId}/><div><strong>{p.title}</strong><small>{new Date(p.at).toLocaleString()} · {p.digest.slice(0,12)}…</small></div><span>{pct(p.probability)} YES</span><span>Outcome {p.outcome?'YES':'NO'}</span><b>{p.brier.toFixed(3)}</b></article>)}{!state.proofs.length?<p className="table-empty">Collector is waiting for canonical forecasts to resolve.</p>:null}</div></section>}
function PortfolioPage(){const{account,connect}=useWalletContext();const[portfolio,setPortfolio]=useState<Portfolio|null>(null);const[error,setError]=useState('');const[busy,setBusy]=useState(false);const refresh=useCallback(async()=>{if(!account)return;setBusy(true);try{setPortfolio(await loadPortfolio(account));setError('')}catch(e){setError(e instanceof Error?e.message:'Portfolio unavailable')}finally{setBusy(false)}},[account]);useEffect(()=>{void refresh()},[refresh]);if(!account)return <section className="empty-state"><span className="empty-glyph">⌁</span><h1>Your positions, in one place.</h1><p>Connect a Shannon testnet wallet to view positions, open orders, fills and claimable winnings.</p><button className="button primary" onClick={connect}><Wallet size={17}/> Connect wallet</button></section>;async function claim(){setBusy(true);try{const r=await redeemAll(account!);setError(`Redeemed ${r.count} position(s): ${r.hash.slice(0,12)}…`);await refresh()}catch(e){setError(e instanceof Error?e.message:'Redeem failed')}finally{setBusy(false)}}return <section className="inner-page"><div className="page-title-actions"><div><span className="eyebrow">YOUR TESTNET WALLET</span><h1>Portfolio.</h1><p className="page-lead">Indexer balances can lag. Transaction receipts confirm writes; the portfolio catches up shortly after.</p></div><div className="portfolio-actions"><button className="button outline" onClick={refresh} disabled={busy}><RefreshCw className={busy?'spin':''} size={16}/> Refresh</button><button className="button primary" onClick={claim} disabled={busy}>Claim available</button></div></div>{error?<p className="notice">{error}</p>:null}<div className="portfolio-summary"><div><span>Positions</span><strong>{portfolio?.positions.length||0}</strong></div><div><span>Open orders</span><strong>{portfolio?.openOrders.length||0}</strong></div><div><span>Recent fills</span><strong>{portfolio?.trades.length||0}</strong></div></div><div className="portfolio-section"><div className="panel-title"><span>Positions</span></div>{portfolio?.positions.map(p=><div className="portfolio-row" key={`${p.market.id}-${p.outcomeIndex}`}><span><strong>{p.market.asset} · {p.outcomeIndex===0?'YES':'NO'}</strong><small>{p.market.question}</small></span><b>{Number(formatUnits(BigInt(p.balance),p.market.quoteDecimals)).toFixed(2)} shares</b></div>)}{!portfolio?.positions.length?<p className="table-empty">No outcome shares found.</p>:null}</div><div className="portfolio-section"><div className="panel-title"><span>Open orders</span></div>{portfolio?.openOrders.map(o=><div className="portfolio-row" key={o.id}><span><strong>{o.market.asset} · {o.side}</strong><small>{o.market.question}</small></span><b>{Number(formatUnits(BigInt(o.quantityRemaining),o.market.quoteDecimals)).toFixed(2)} shares</b><button className="button outline tiny" disabled={busy} onClick={async()=>{setBusy(true);try{await cancelOrder(o.market.poolAddress,o.orderId,account!);await refresh()}catch(e){setError(e instanceof Error?e.message:'Cancel failed')}finally{setBusy(false)}}}>Cancel</button></div>)}{!portfolio?.openOrders.length?<p className="table-empty">No resting orders.</p>:null}</div></section>}
export default function Arena(){const{state,error,mode,load,retry,retrying}=useArena();const[account,setAccount]=useState<Address|null>(null);const[walletError,setWalletError]=useState('');const connect=useCallback(async()=>{try{setAccount(await connectWallet());setWalletError('')}catch(e){setWalletError(e instanceof Error?e.message:'Wallet connection failed')}},[]);return <WalletContext.Provider value={{account,connect}}><div className="app-shell"><AppHeader account={account} onConnect={connect} mode={mode}/><DataBanner state={state} error={error||walletError} mode={mode}/><main id="main-content" className="app-main">{!state?<section className="loading-state"><RefreshCw className="spin"/><span>Opening the arena…</span></section>:<Routes><Route index element={<ArenaHome state={state} onRetry={retry} retrying={retrying}/>}/><Route path="agents" element={<AgentsPage state={state}/>}/><Route path="leaderboard" element={<Scorecard state={state}/>}/><Route path="portfolio" element={<PortfolioPage/>}/><Route path="*" element={<EmptyState live={false}/>}/></Routes>}</main><footer className="app-footer"><span>DreamCurve · Somnia Shannon testnet</span><Link to="/methodology">Methodology</Link><button onClick={load}>Refresh data</button></footer></div></WalletContext.Provider>}
